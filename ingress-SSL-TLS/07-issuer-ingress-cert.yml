# ---
# apiVersion: cert-manager.io/v1
# kind: ClusterIssuer
# metadata:
#   name: letsencrypt-prod
#   namespace: cert-manager
# spec:
#   acme:
#     # You must replace this email address with your own.
#     email: benadeyele@gmail.com
#     server: https://acme-v02.api.letsencrypt.org/directory
#     privateKeySecretRef:
#       name: adeyeletechhub-tls
#     solvers:
#       - http01:
#           ingress:
#             class: nginx

# ---

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: my-certificate
spec:
  secretName: production-cluster-issuer
  dnsNames:
    - adeyeletechhub.com.ng
  issuerRef:
    name: production
    kind: ClusterIssuer

# ---

# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: my-ingress
# spec:
#   tls:
#     - hosts:
#         - sock-shop.adeyeletechhub.com.mg
#       secretName: adeyeletechhub-tls
#   rules:
#     - host: sock-shop.adeyeletechhub.com.ng
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: front-end
#                 port:
#                   number: 80